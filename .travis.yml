language: java

jdk:
- oraclejdk8

install:
true

before_install:
  - chmod +x mvnw

script:
 ./mvnw clean install
  "mvn cobertura:cobertura"
  
after_success:
- bash <(curl -s https://codecov.io/bash)

cache:
  directories:
  - $HOME/.m2
